sudo: false
language: go
go:
- 1.7
before_install:
- go get github.com/mcuadros/go-syslog.v2
env:
  global:
  matrix:
  - TEST_TYPE=small
install:
  - export TMPDIR=$HOME/tmp
  - mkdir -p $TMPDIR
script:
  - go test -tags=$TEST_TYPE -v
notifications:
  email: false
